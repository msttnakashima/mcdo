# TASK-2026-06-11-001

status: done
owner: spec-architect
created: 2026-06-11
dependencies: []

## ID Rules

Main task ID must match:

```text
^TASK-[0-9]{4}-[0-9]{2}-[0-9]{2}-[0-9]{3}$
```

Sub spec ID must match:

```text
^TASK-[0-9]{4}-[0-9]{2}-[0-9]{2}-[0-9]{3}-[A-Z]$
```

Spec filename must match:

```text
^specs/TASK-[0-9]{4}-[0-9]{2}-[0-9]{2}-[0-9]{3}(-[A-Z])?\.spec\.md$
```

Invalid IDs or filenames block routing and implementation. Do not silently auto-normalize them.

## Main Task

### Goal

Deliver the McDonald's "Hello World" static web app that displays exactly `hello from mcdo`.

### Scope In

- A single static browser page that displays the exact PRD text and has no navigation or external links.

### Scope Out

- Frontend frameworks, package managers, build tooling, analytics, images, brand assets, backend services, deployment provider configuration, domain setup, and SSL setup.

### Dependencies

- none.

### Specialist Routing

Primary specialist: `frontend-domain-specialist`

Reason: The main deliverable is a static frontend browser surface with exact text rendering and page-level validation.

### Sub Specs

- `TASK-2026-06-11-001-A`: create the static single-page app files and exact visible content.
- `TASK-2026-06-11-001-B`: apply the minimalist centered responsive layout and complete browser validation.

## Acceptance Criteria

- The app consists of one static page.
- The visible page text is exactly `hello from mcdo`.
- The page contains no navigation and no external links.
- The layout is clean, centered, and usable on desktop and mobile viewports.
- The page loads without browser console errors.
- No framework, package manager, or build system is introduced.

## Validation

- No repo command exists yet.
- Open the static page in a desktop browser and confirm the visible text is exactly `hello from mcdo`.
- Open the static page in a mobile viewport and confirm the visible text is exactly `hello from mcdo`.
- Inspect the page and confirm there are no `a` elements or navigation controls.
- Confirm zero console errors on page load.

## Review Notes

- Risks: accidental text mismatch, adding unnecessary framework/tooling, adding links or extra visible content, browser validation skipped.
- High-conflict files: root app entry files if introduced by implementation.
- Parallel-safe: no, because both sub specs may touch the same static page files and should serialize on one branch.

## Handoff

- Implementation summary: Added a minimalist static `index.html` page that renders exactly `hello from mcdo`, then applied centered responsive system-font styling without adding frameworks, package managers, build tooling, navigation, external links, assets, or extra visible copy.
- Validation evidence: Subagents validated desktop and mobile browser loads with exact text, zero `a` elements, zero `nav` elements, and zero console errors. Main-agent validation loaded the page through a temporary static server with isolated headless Chrome, confirmed rendered body text exactly `hello from mcdo`, confirmed `a_count=0` and `nav_count=0`, and generated a 390x844 mobile screenshot showing centered text.
- Review status: Main-agent review complete; ready for PR.
