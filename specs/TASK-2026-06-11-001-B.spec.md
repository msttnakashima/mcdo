# TASK-2026-06-11-001-B

status: done
owner: ui-ux-frontend-design-specialist
created: 2026-06-11
dependencies:
  - TASK-2026-06-11-001-A

## ID Rules

Main task ID must match:

```text
^TASK-[0-9]{4}-[0-9]{2}-[0-9]{2}-[0-9]{3}$
```

Sub spec ID must match:

```text
^TASK-[0-9]{4}-[0-9]{2}-[0-9]{2}-[0-9]{3}-[A-Z]$
```

Spec filename must match:

```text
^specs/TASK-[0-9]{4}-[0-9]{2}-[0-9]{2}-[0-9]{3}(-[A-Z])?\.spec\.md$
```

Invalid IDs or filenames block routing and implementation. Do not silently auto-normalize them.

## Main Task

### Goal

Polish the static page into a clean centered desktop and mobile layout and validate the browser surface.

### Scope In

- Add or refine minimal CSS for centered responsive presentation of the exact text `hello from mcdo`.

### Scope Out

- Text changes, additional visible copy, navigation, links, images, logos, brand assets, animations, framework adoption, deployment configuration, and performance tooling.

### Dependencies

- `TASK-2026-06-11-001-A`: the static page source must exist before visual polish and browser validation.

### Specialist Routing

Primary specialist: `ui-ux-frontend-design-specialist`

Reason: This sub spec owns layout, typography, responsive viewport behavior, and visual browser checks for the minimalist page.

### Sub Specs

- none.

## Acceptance Criteria

- The text `hello from mcdo` is visually centered in the viewport on desktop.
- The text `hello from mcdo` is visually centered in the viewport on a mobile viewport.
- The typography uses standard system fonts and remains readable without overflow.
- The page remains a single static page with no navigation and no external links.
- The visible text remains exactly `hello from mcdo`.
- Browser validation shows zero console errors on page load.

## Validation

- No repo command exists yet.
- Use a desktop browser viewport to confirm centered layout, exact visible text, no links, and zero console errors.
- Use a mobile browser viewport to confirm centered layout, exact visible text, no links, and zero console errors.
- If a local server is needed for validation, use a temporary static file server only and do not add server tooling to the repository.

## Review Notes

- Routing note: Dependency `TASK-2026-06-11-001-A` is `done`, so this sub spec can route for implementation.
- Risks: visual polish introduces extra content, alters the exact text, or hides overflow on small screens.
- High-conflict files: static page file or stylesheet created by `TASK-2026-06-11-001-A`.
- Parallel-safe: no, because it depends on and may edit the same files as `TASK-2026-06-11-001-A`.

## Handoff

- Implementation summary: Applied minimal inline CSS in `index.html` to center the exact page text in the viewport using grid layout, system typography, readable sizing, centered alignment, and responsive padding without adding visible copy, links, navigation, assets, frameworks, or tooling.
- Validation evidence: Loaded `index.html` locally in browser tooling at desktop and mobile viewports; visible body text remained exactly `hello from mcdo`; text was centered in both viewports without overflow; DOM contained zero `a` elements and zero `nav` elements; console error count was zero.
- Review status: Ready for main-agent review.
